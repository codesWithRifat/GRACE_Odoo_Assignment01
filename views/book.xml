<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_library_book_tree" model="ir.ui.view">
        <field name="name">library.book.tree</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="genre"/>
                <field name="author_ids"/>
                <field name="numOfcopys"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_library_book_form" model="ir.ui.view">
        <field name="name">library.book.form</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="genre"/>
                        <field name="author_ids" widget="many2many_tags"/>
                        <field name="numOfcopys"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="view_library_book_kanban" model="ir.ui.view">
        <field name="name">library.book.kanban</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_example">
                <field name="name"/>
                <field name="genre"/>
                <field name="author_ids"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <strong><field name="name"/></strong><br/>
                            <span><field name="genre"/></span><br/>
                            <span>Copys:<field name="numOfcopys"/></span><br/>
                            <span>
                                <t t-foreach="record.author_ids.raw_value" t-as="author">
                                    <span t-esc="author[1]" class="badge badge-info" style="margin: 2px;"/>
                                </t>
                            </span>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Action -->
    <record id="action_library_book" model="ir.actions.act_window">
        <field name="name">Library Books</field>
        <field name="res_model">library.book</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first book record
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <menuitem id="library_menu_root" name="Library Management" sequence="10"/>
    <menuitem id="library_book_menu" name="Books" parent="library_menu_root" action="action_library_book"/>
</odoo>
